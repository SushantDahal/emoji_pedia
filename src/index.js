import React from "react";
import ReactDOM from "react-dom";
import App from "./emoji_components/App";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <>
    <App />
  </>
);
